"use client";
import StaffNavigation from "@/app/components/staff/StaffNavigation";
import { StaffAction, StaffHero, StaffSection, StaffShell, StaffStat } from "@/app/components/staff/StaffUI";
import { useStaffWorkspace } from "@/app/components/staff/useStaffWorkspace";
export default function StaffProfilePage(){const {profile,staffFiles,loading,warning}=useStaffWorkspace(); return <StaffShell><div className="mx-auto max-w-[1500px] space-y-6"><StaffHero name={profile?.fullName||"Staff Member"} designation="My Staff File" description="A secure read-only summary of the personnel information linked to your authenticated account." actions={<><StaffAction href="/profile" tone="blue">Update Personal Profile</StaffAction><StaffAction href="/staff" tone="slate">Overview</StaffAction></>}/><StaffNavigation/>{warning?<div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm font-bold text-amber-900">{warning}</div>:null}<section className="grid gap-4 sm:grid-cols-3"><StaffStat label="File Status" value={loading?"—":staffFiles.length?"Registered":"Pending"} tone={staffFiles.length?"emerald":"amber"}/><StaffStat label="Department" value={profile?.department||"—"} tone="blue"/><StaffStat label="Role" value={profile?.role||"—"} tone="violet"/></section><StaffSection title="Employment Profile" eyebrow="Personal record"><dl className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{[["Full Name",profile?.fullName],["Email",profile?.email],["Phone",profile?.phone||"Not provided"],["Department",profile?.department],["Designation",profile?.designation],["Primary Role",profile?.role]].map(([label,value])=><div key={label} className="rounded-2xl border border-slate-200 bg-slate-50 p-4"><dt className="text-xs font-black uppercase tracking-wider text-slate-500">{label}</dt><dd className="mt-2 break-words text-sm font-black text-slate-950">{value||"—"}</dd></div>)}</dl><div className="mt-5 rounded-2xl border border-blue-200 bg-blue-50 p-4 text-sm font-semibold leading-6 text-blue-900">Confidential personnel documents remain under HR control. Staff can view only the approved summary linked to their own account.</div></StaffSection></div></StaffShell>}
