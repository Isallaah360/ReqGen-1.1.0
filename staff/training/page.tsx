"use client";
import StaffNavigation from "@/app/components/staff/StaffNavigation";
import { StaffAction, StaffBadge, StaffEmpty, StaffHero, StaffSection, StaffShell, StaffStat } from "@/app/components/staff/StaffUI";
import { useStaffWorkspace } from "@/app/components/staff/useStaffWorkspace";
import { dateText, text } from "@/app/components/enterprise/data";
export default function StaffTrainingPage(){const {profile,training,loading,warning}=useStaffWorkspace(); const completed=training.filter(r=>text(r.status).toLowerCase().includes("complete")); return <StaffShell><div className="mx-auto max-w-[1500px] space-y-6"><StaffHero name={profile?.fullName||"Staff Member"} designation="My Learning & Development" description="View capacity-building programmes, completion status, certificates and learning history." actions={<StaffAction href="/staff" tone="slate">Overview</StaffAction>}/><StaffNavigation/>{warning?<div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm font-bold text-amber-900">{warning}</div>:null}<section className="grid gap-4 sm:grid-cols-2"><StaffStat label="Training Records" value={loading?"—":training.length} tone="blue"/><StaffStat label="Completed" value={loading?"—":completed.length} tone="emerald"/></section><StaffSection title="My Training History" eyebrow="Staff development">{training.length===0?<StaffEmpty title="No training record" description="Programmes assigned to you will appear here when HR records participation."/>:<div className="space-y-3">{training.map(r=><div key={text(r.id)} className="rounded-2xl border border-slate-200 bg-white p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="font-black text-slate-950">{text(r.programme_title,text(r.title,"Training Programme"))}</div><div className="mt-1 text-sm font-semibold text-slate-600">{text(r.provider,"IET")} · {text(r.start_date,"—")} to {text(r.end_date,"—")}</div><div className="mt-1 text-xs font-bold text-slate-400">Recorded {dateText(r.created_at)}</div></div><StaffBadge tone="blue">{text(r.status,"Assigned")}</StaffBadge></div></div>)}</div>}</StaffSection></div></StaffShell>}
