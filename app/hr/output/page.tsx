"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { supabase } from "@/lib/supabaseClient";
import HRAccessGuard from "@/app/components/hr/HRAccessGuard";
import { PrimaryButton, SectionCard, StrategicHero, StrategicNavigation, StrategicShell } from "@/app/components/hr/HRStrategicUI";

type ReportKey = "development" | "capacity" | "seminar" | "kpi" | "assessment" | "officers";
const reportNames: Record<ReportKey, string> = { development: "Staff Development Report", capacity: "Department Capacity Report", seminar: "Weekly Seminar Attendance Report", kpi: "Department KPI Report", assessment: "Annual 360° Assessment Report", officers: "HR Officer Workload Report" };
export default function HROutputPage() {
  const [selected, setSelected] = useState<ReportKey>("development"); const [rows, setRows] = useState<Record<string, unknown>[]>([]); const [loading, setLoading] = useState(false);
  const load = useCallback(async (key: ReportKey) => { setLoading(true); const map: Record<ReportKey, [string, string]> = { development: ["hr_staff_training_programmes", "*"], capacity: ["hr_department_capacity_programmes", "*"], seminar: ["hr_seminar_attendance", "*"], kpi: ["hr_department_kpis", "*"], assessment: ["hr_assessment_results", "*"], officers: ["hr_request_assignments", "*"] }; const [table, select] = map[key]; const { data } = await supabase.from(table).select(select).limit(1000); setRows((data || []) as Record<string, unknown>[]); setLoading(false); }, []);
  useEffect(() => { void load(selected); }, [load, selected]);
  const headers = useMemo(() => Array.from(new Set(rows.flatMap((row) => Object.keys(row)))).slice(0, 12), [rows]);
  function exportCsv() { const csv = [headers.join(","), ...rows.map((row) => headers.map((header) => `"${String(row[header] ?? "").replace(/"/g, '""')}"`).join(","))].join("\n"); const blob = new Blob([csv], { type: "text/csv;charset=utf-8" }); const url = URL.createObjectURL(blob); const anchor = document.createElement("a"); anchor.href = url; anchor.download = `${selected}-report.csv`; anchor.click(); URL.revokeObjectURL(url); }
  function print() { window.print(); }
  return <HRAccessGuard bossOnly><StrategicShell><StrategicHero eyebrow="HR Reports & Output" title="HR Print & Export Centre" description="Generate standardized printable and exportable records for development, capacity, seminar attendance, KPI, assessments and HR officer workload." /><StrategicNavigation /><SectionCard title="Report Selection" eyebrow="Output Configuration"><div className="grid gap-4 md:grid-cols-[1fr_auto_auto]"><select value={selected} onChange={(e) => setSelected(e.target.value as ReportKey)} className="rounded-xl border-2 border-slate-200 bg-white px-4 py-3 font-black text-slate-900 outline-none focus:border-blue-600">{Object.entries(reportNames).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select><PrimaryButton tone="violet" onClick={print}>Print Report</PrimaryButton><PrimaryButton tone="emerald" onClick={exportCsv} disabled={!rows.length}>Export CSV</PrimaryButton></div></SectionCard><section id="hr-output-report" className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm print:shadow-none"><div className="border-b-2 border-slate-900 pb-4 text-center"><p className="text-xs font-black uppercase tracking-[0.2em] text-slate-600">Islamic Education Trust</p><h2 className="mt-2 text-2xl font-black text-slate-950">{reportNames[selected]}</h2><p className="mt-1 text-xs font-semibold text-slate-500">Generated {new Date().toLocaleString("en-NG")}</p></div><div className="mt-5 overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-xs"><thead className="bg-slate-100"><tr>{headers.map((header) => <th key={header} className="px-3 py-2 text-left font-black uppercase text-slate-700">{header.replace(/_/g, " ")}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{loading ? <tr><td colSpan={Math.max(headers.length, 1)} className="px-4 py-10 text-center font-bold text-slate-500">Loading report...</td></tr> : rows.map((row, index) => <tr key={String(row.id || index)}>{headers.map((header) => <td key={header} className="max-w-52 truncate px-3 py-2 font-semibold text-slate-700">{String(row[header] ?? "")}</td>)}</tr>)}</tbody></table></div></section></StrategicShell></HRAccessGuard>;
}
