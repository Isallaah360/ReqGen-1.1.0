"use client";

/**
 * Administration navigation is provided by the adopted global ReqGen sidebar.
 * Existing Admin pages still import this component, so it intentionally renders
 * nothing to avoid a second Administration Menu inside the workspace.
 */
export default function AdminNavigation() {
  return null;
}
