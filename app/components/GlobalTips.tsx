"use client";

/**
 * Phase 6 stabilisation: contextual guidance is consolidated in the shell
 * Important Note control. Floating hover tooltips are intentionally disabled
 * so they never obscure live workspaces or action controls.
 */
export default function GlobalTips() {
  return null;
}
