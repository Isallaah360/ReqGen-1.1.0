"use client";

/**
 * Staff navigation is provided by the adopted global ReqGen sidebar.
 * Keeping this component as a no-op preserves existing page imports while
 * preventing duplicated in-page navigation tabs/cards.
 */
export default function StaffNavigation() {
  return null;
}
